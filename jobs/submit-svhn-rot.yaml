description: Training SVHN Robustness [L2 matched, Rotated]

target:
  vc: msrlabspvc9
  cluster: eu1

environment:
  image: microsoft_pytorch:v1.2.0_gpu_cuda9.0_py36_release_gpuenv_hvd0.16.2
  registry: phillyregistry.azurecr.io
  setup:
  - pip install torchnet --user
  - pip install statsmodels --user
  - pip install tqdm --user

code:
  local_dir: $CONFIG_DIR/../

data:
  local_dir: $CONFIG_DIR/../data/
  remote_dir: datasets/

search:
  job_template:
    name: svhn
    sku: G1
    command:
    - python3 -m src.train
      --noise={noise}
      --sigma={sigma}
      --experiment-name=svhn_{noise}_{sigma}
      --dataset=svhn
      --p=2
      --rotate
    - python3 -m src.test
      --noise={noise}
      --sigma={sigma}
      --experiment-name=svhn_{noise}_{sigma}
      --dataset=svhn
      --p=2
      --rotate
  type: grid
  max_trials: 24
  params:
    - name: sigma
      spec: discrete
      values: [0.15, 0.25, 0.5, 0.75, 1.0, 1.25]
    - name: noise
      spec: discrete
      values: ["GaussianNoise", "UniformNoise", "LaplaceNoise", "LomaxNoise"]

